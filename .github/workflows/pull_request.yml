on: pull_request

name: Build and Test

jobs:
  build:
    name: Build and Test
    runs-on: macOS-10.14
    
    steps:
    - uses: actions/checkout@master
    
    - name: Build
      run:
        npx install
    
    - name: Test
      run:
        npx test
        