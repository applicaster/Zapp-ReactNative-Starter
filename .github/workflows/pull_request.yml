on: pull_request

name: Test

jobs:
  build:
    name: Test
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@master
 
    - uses: actions/setup-node@v1
      with:
        node-version: '8.x'
    - run: npm install
    - run: npm test
