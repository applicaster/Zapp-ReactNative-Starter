on: pull_request

name: Test

jobs:
  build:
    name: Test
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@master
 
    - name: NPM Install
      uses: actions/npm@master
      with:
        args: install
    
    - name: NPM Test
      uses: actions/npm@master
      with:
        args: test

    - name: install ruby
      uses: mscoutermarsh/ruby-action-recipe@master
      run:
        apt install linuxbrew-wrapper
        brew tap applicaster/tap
        brew install zappifest
